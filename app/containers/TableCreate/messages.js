/*
 * TableCreate Messages
 *
 * This contains all the text for the TableCreate container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableCreate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableCreate container!',
  },
});
