/*
 * TablePage Messages
 *
 * This contains all the text for the TablePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TablePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Listado de mesas',
  },
});
